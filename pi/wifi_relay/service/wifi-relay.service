[Unit]
Description=WiFi Relay API
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/wifi-relay

ExecStart=/opt/wifi-relay/venv/bin/uvicorn main:app --host 0.0.0.0 --port 4005

Restart=always
RestartSec=3
Environment=PYTHONUNBUFFERED=1

[Install]
WantedBy=multi-user.target
